Base64 编解码工具支持：

- **编码**：将普通文本编码为 Base64 格式，支持 UTF-8 中文
- **解码**：将 Base64 字符串解码为原始文本
- **URL Safe 模式**：切换 base64url 编码（`+` → `-`，`/` → `_`，去除 `=` 填充）
- **实时转换**：输入即转换，无需点击按钮
- **独立双卡**：编码区与解码区独立操作，互不干扰
- **一键复制**：每个结果区域均有复制按钮

常用于 URL 参数传递、JWT 调试、API 数据传输、简单数据编码等场景。